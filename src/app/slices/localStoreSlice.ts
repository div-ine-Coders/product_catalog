import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const localStoreSlice = createSlice({
  name: 'localStoreSlice',
  initialState: initialState,
  reducers: {},
});

export default localStoreSlice.reducer;
